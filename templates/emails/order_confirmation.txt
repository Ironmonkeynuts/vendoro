Hi {{ order.user.get_full_name|default:order.user.username }},

Thanks for your purchase — your order is confirmed.

Order #: {{ order.id }}
Placed: {% if placed_at %}{{ placed_at|date:"Y-m-d H:i" }}{% else %}N/A{% endif %}
Status: {{ status_display|default:"N/A" }}

Items:
{% for it in items %}
- {{ it.quantity }} × {{ it.product.title }} @ {{ currency }}{{ it.unit_price|floatformat:2 }}{% if it.product.shop %} ({{ it.product.shop.name }}){% endif %}
{% empty %}
- No items recorded.
{% endfor %}

Total: {{ currency }}{{ order.total_amount|floatformat:2 }}

We’ll email you again when your order ships.
If you have any questions, just reply to this message.

Thanks,
The {{ order.shop.name|default:"Vendoro" }} team